package com.craxiom.networksurvey.ui.cellular

/**
 * Enum to distinguish between different map view contexts
 */
enum class MapContext {
    TOWER_MAP,      // Default tower map view
    SURVEY_MONITOR  // Active survey monitoring view
}