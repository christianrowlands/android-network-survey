[versions]
#ksp = "2.3.5"  # KSP applied via classpath in root build.gradle buildscript block
kotlin = "2.3.10"

[plugins]
compose-compiler = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
kotlin-parcelize = { id = "org.jetbrains.kotlin.plugin.parcelize", version.ref = "kotlin" }
#google-devtools-ksp = { id = "com.google.devtools.ksp", version.ref = 'ksp' }  # Applied via classpath instead
